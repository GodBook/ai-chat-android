# AI BOTOY 项目交接文档

文档版本：2026-09-10  
对应代码版本：`1.5.4` / `versionCode 21`  
仓库：<https://github.com/GodBook/ai-chat-android>  
当前发布：<https://github.com/GodBook/ai-chat-android/releases/tag/v1.5.4>

本文面向接手项目的 Android 开发者，目标是让新维护者能够在没有额外口头说明的情况下完成环境准备、源码理解、本地验证、版本发布和常见问题处理。

## 一、项目定位

AI BOTOY 是一个原生 Android AI 聊天应用，支持 OpenAI Chat Completions 兼容接口。主要能力包括：

- 多会话聊天、搜索、重命名、删除和纯文本导出。
- SSE 流式回复、停止生成、失败重试和中断恢复。
- 图片输入，图片保存在应用私有目录后以 Base64 发送给视觉模型。
- CommonMark Markdown、GFM 表格、代码块、链接和原生文字选择。
- 后台截屏问答（可自选音量下键或音量上下键同时按）、悬浮窗回答和选择/判断题简答模式。
- HTTPS 在线更新，校验清单、SHA-256、包名、版本号和签名后交给系统安装器。

应用包名为 `com.example.aichat`，最低 Android 版本为 29（Android 10），目标和编译版本为 36。

## 二、当前状态

`1.5.4` 已发布。当前版本的功能改动是：

- 修复模型快捷选择即时生效：点击模型“快捷选择”芯片（如 V4.1 Flash 限时）时立即异步持久化到 DataStore，不再依赖用户必须滑动到页面最底部点击“保存设置”。
- 增加设置页面退出自动保存（`BackHandler` 与顶栏返回键）：退出设置界面时若检测到未保存的表单修改（模型、接口地址、密钥、视觉开关等），自动校验并执行保存后再返回，彻底解决“修改或选择模型后直接返回导致配置被重置”的问题；若校验失败会显示具体错误，再次返回可安全退出。

`1.5.3` 的改动：

- 修复“模型失效自动回退”开关即时持久化问题：在设置页拨动该开关时立即异步写入 DataStore（键 `auto_fallback_enabled`），不再依赖页面底部的“保存设置”大按钮；退出设置界面再重新进入仍保持用户选择的关闭状态。
- 解决模型静默回退问题：关闭该开关后，请求失败绝不回退到 `deepseek-v4-flash`，严格保证用户所选模型的请求一致性。

`1.5.2` 的改动：

- 修复模型误回退：`isModelUnavailable()` 原来只要错误信息里出现“模型”“invalid”等任意一个词就会切到备用模型，导致普通请求错误（如参数无效、限流文案带“模型”二字）也会把 v4.1 悄悄换成 v4。现在必须同时出现“模型”类词和“不存在/过期/下线”类词（404 除外）才判定为模型不可用。
- 设置页模型名称下新增“模型失效自动回退”开关（DataStore 键 `auto_fallback_enabled`，默认开）。关闭后 `streamChat()` 只用用户填写的模型，绝不切换。
- 应用启动时自动清理更新缓存：`AppUpdateManager.deleteInstalledUpdateApks()` 删除版本号不高于当前应用的 `ai-chat-<versionCode>.apk`（含 `.bak`）和残留的 `.download.apk` 临时文件；尚未安装的更高版本 APK 会保留。由 `AiChatApplication.onCreate()` 在后台线程调用。

`1.5.1` 的改动：

- 设置页新增“快捷选择”模型芯片（`MODEL_PRESETS`）：V4.1 Flash 限时、V4 Flash 备用、DeepSeek Chat、DeepSeek Reasoner、GPT-4o mini、GPT-4.1 mini。点一下即填入模型名，手写输入框保留。
- 芯片带预设接口地址时才会顺带改接口地址，且只覆盖官方默认地址；自建网关/中转站地址不会被改写（`canReplaceEndpointForPreset()`）。

`1.5.0` 的改动：

- 默认接口地址改为 `https://api.deepseek.com/v1`，默认模型改为 `deepseek-v4.1-flash-expires-on-0910`。
- 默认模型不可用（例如定时模型过期）时，`OpenAiCompatibleClient.streamChat()` 会自动改用 `deepseek-v4-flash` 重试一次。

`1.4.9` 的改动：

- 对发送和重试请求增加约 48,000 字符的上下文上限，优先保留最近消息。
- 长代码块默认折叠，支持展开/收起；复制操作仍复制完整代码。
- 简版回答模式支持多选题，可同时点亮多个选项方块。
- 后台截图快捷键可在设置中选择：音量下键，或音量上 + 下键同时按（`ScreenshotTrigger`，存于 DataStore 的 `screenshot_trigger`）。

当前尚未完成但值得继续安排的工作：

- 真正的历史消息分页加载，而不只是限制发送给模型的上下文。
- 网络失败自动重试和指数退避。
- 设置页的模型连接测试。
- 更完整的聊天备份和恢复。
- 后台截屏的隐私遮挡和更细的任务模板。

## 三、技术栈和目录

### 技术栈

- Kotlin 2.1.21、Jetpack Compose、Material 3。
- Android Gradle Plugin 8.10.1、Gradle Wrapper 8.11.1。
- Room 2.7.1：聊天会话和消息。
- DataStore Preferences：普通配置和更新清单地址。
- Android Keystore / `EncryptedSharedPreferences`：API Key。
- OkHttp 4.12：模型接口、SSE 和更新下载。
- Kotlin Serialization：JSON 编解码。
- Coil 3：本地图片展示。
- CommonMark 0.24：Markdown 解析。

### 主要目录

```text
app/src/main/java/com/example/aichat/
├── MainActivity.kt                 应用入口
├── AiChatApplication.kt            Application 与 AppContainer
├── ui/
│   ├── 应用外壳.kt                  导航、Snackbar、系统权限衔接
│   ├── MainViewModel.kt             UI 状态和用户操作编排
│   ├── 聊天列表界面.kt              会话列表
│   ├── 聊天界面.kt                  聊天页面、输入框、滚动逻辑
│   ├── 消息渲染.kt                  消息气泡、Markdown 展示
│   ├── MarkdownParser.kt            CommonMark 到 UI 模型
│   └── 设置界面.kt                  配置、后台截屏、在线更新
├── data/
│   ├── model/                       领域模型和状态枚举
│   ├── local/                       Room、DataStore、Keystore、图片文件
│   ├── network/                     SSE 和 OpenAI 兼容客户端
│   ├── repository/                  ChatRepository 和默认实现
│   └── update/                      更新清单、下载和安装校验
└── background/                      截屏服务、无障碍服务、悬浮窗
```

测试位于 `app/src/test` 和 `app/src/androidTest`。GitHub Actions 发布流程位于 `.github/workflows/发布.yml`。

## 四、启动和核心数据流

`AiChatApplication.onCreate()` 创建 `AppContainer`，集中初始化数据库、配置存储、API Key 存储、图片存储、网络客户端、更新管理器和聊天仓储。`MainActivity` 创建 `MainViewModel`，Compose 界面通过 `uiState` 订阅状态。

一次普通发送的流程如下：

1. `聊天界面.kt` 收集文本和图片路径，调用 `MainViewModel.send()`。
2. `DefaultChatRepository.sendMessage()` 校验配置和图片能力。
3. 用户消息和空的助手消息在同一 Room 事务中写入。
4. `OpenAiCompatibleClient.streamChat()` 发送 `/chat/completions` 请求并解析 SSE。
5. 每批增量文本周期性写回 Room，界面通过 Flow 实时刷新。
6. `[DONE]` 到达后写入 `SENT`；停止、断线或服务端错误分别写入 `INTERRUPTED` 或 `FAILED`。
7. 应用启动时 `recoverInterruptedMessages()` 把遗留的 `SENDING`/`STREAMING` 标为可重试的中断状态。

后台截屏流程为：设置里选中的音量键组合（或设置页测试按钮）触发截图服务，保存图片到 `filesDir/chat-images`，创建名为“截屏问答”的新会话，发送图片和自定义提示词，最后由悬浮窗管理器展示结果。

`VolumeDownAccessibilityService` 通过 `FLAG_REQUEST_FILTER_KEY_EVENTS` 接收全局音量键事件。`onKeyEvent()` 用 `volumeKeyPressedAt` 记录两个音量键的按下时间，重复事件（`repeatCount > 0`）只刷新时间、不触发截图，松开按键时清空对应槽位：

- `ScreenshotTrigger.VOLUME_DOWN`：音量下键的首次按下即触发。
- `ScreenshotTrigger.VOLUME_UP_DOWN`：另一个键处于“按住”状态（2 秒内的按下或重复事件）时才触发，并用 `pairTriggerConsumed` 避免一直按住时重复截图。

两者都保留 700 ms 防抖，且始终 `return false` 不吞掉按键，系统音量调节不受影响。

## 五、数据和安全边界

### Room

数据库文件名为 `ai_chat.db`。当前版本为 3，迁移链为 `MIGRATION_1_2` 和 `MIGRATION_2_3`。不要直接修改已有字段或删除迁移；需要改表时新增迁移并补充 `androidTest`。

主要表：

- `chat_conversations`：会话 ID、标题、创建时间、更新时间。
- `chat_messages`：角色、正文、图片路径、状态、请求 ID、会话 ID和错误信息。

删除会话时会先删除消息，再删除不再被其他消息引用的图片。共享图片路径不能直接无条件删除。

### DataStore 和 API Key

普通模型配置存于 `provider_config`，包括 Base URL、模型、视觉开关、截屏提示词、悬浮窗外观和简答模式。更新清单地址由 `UpdateConfigStore` 单独保存。

默认接口地址 `DEFAULT_BASE_URL` 和默认模型 `DEFAULT_MODEL`、`FALLBACK_MODEL` 统一定义在 `data/model/ChatMessage.kt`，`ConfigStore` 与 `ProviderConfig` 都引用它们，改默认值只需要改这一处。`modelCandidatesFor()` 返回要依次尝试的模型，只有登记过的模型才有备用模型，用户自定义的模型不会被静默替换。

API Key 不进入 Room、DataStore、日志或导出内容，读取和保存通过 `ApiKeyStore` 完成。任何调试日志都不得打印请求 Header、完整 URL 查询参数、API Key 或图片 Base64。

### 网络安全

生产环境只允许 HTTPS。Manifest 关闭明文流量，模型请求和更新请求也分别校验 HTTPS。`allowInsecureHttp` 和 `allowInsecureHttpForTests` 只能用于测试，不能在生产容器中打开。

## 六、本地开发

需要：

- JDK 17，必须包含 `jlink`。
- Android SDK 36，包括 `platforms;android-36` 和 `build-tools;36.0.0`。
- Windows 下建议使用仓库自带的 `gradlew.bat`。

常用命令：

```powershell
.\gradlew.bat :app:testDebugUnitTest --no-daemon
.\gradlew.bat :app:lintDebug --no-daemon
.\gradlew.bat :app:assembleDebug --no-daemon
.\gradlew.bat :app:assembleRelease --no-daemon
```

Debug APK 位于 `app/build/outputs/apk/debug/app-debug.apk`。Release 构建默认未使用生产签名，不能直接用于覆盖已安装的正式版本。

如果遇到 `JdkImageTransform` 或 `jlink executable does not exist`，说明 `JAVA_HOME` 指向了 JRE、JetBrains Runtime 或不完整 JDK。切换到包含 `bin/jlink.exe` 的 JDK 17 后再执行。

## 七、测试策略

单元测试覆盖：

- SSE 分片、结束事件和异常响应。
- OpenAI 兼容请求序列化、HTTP 错误和取消。
- Markdown 解析、聊天筛选和导出。
- 更新清单、HTTPS、签名身份和传输安全。
- 简答模式识别。

仪器测试覆盖：

- Room 迁移。
- 配置存储。
- 多会话删除、清空、预览和图片引用。

改动数据库、网络协议、请求取消、权限或图片清理时，必须补对应测试。UI 改动至少在窄屏、横屏和大字体模式下人工验证。

## 八、GitHub 发布流程

发布前确认：

1. `app/build.gradle.kts` 的 `versionCode` 比线上版本大。
2. `versionName` 与标签完全一致，例如 `1.4.8` 对应 `v1.4.8`。
3. 测试、Lint 和 Release 构建通过。
4. 没有把签名文件、API Key 或本地构建产物提交进 Git。

推荐流程：

```powershell
git status
git add .
git commit -m "发布 1.5.4"
git push origin main
git tag v1.5.4
git push origin v1.5.4
```

推送标签后，`.github/workflows/发布.yml` 会：

1. 使用 JDK 17 和 Android 36。
2. 运行单元测试、Lint 和 Release 构建。
3. 从 GitHub Actions Secrets 恢复固定签名证书。
4. 使用 `apksigner` 签名并校验 APK。
5. 计算 SHA-256，生成 `latest.json`。
6. 创建或更新 GitHub Release，上传 APK 和 `latest.json`。

必须存在以下 GitHub Actions Secrets，名称不能改：

- `AI_CHAT_SIGNING_KEYSTORE_B64`
- `AI_CHAT_SIGNING_KEYSTORE_PASSWORD`
- `AI_CHAT_SIGNING_KEY_ALIAS`
- `AI_CHAT_SIGNING_KEY_PASSWORD`

签名证书是覆盖安装的关键资产。不能更换证书，也不要在仓库、聊天记录或公开日志中暴露 keystore 和密码。

发布后核对：

```powershell
gh release view v1.5.4 --json url,assets
Invoke-RestMethod https://github.com/GodBook/ai-chat-android/releases/latest/download/latest.json
```

清单中的 `versionCode`、`versionName`、`downloadUrl` 和 SHA-256 必须与 Release 资产一致。发布失败时先查看 Actions 日志，不要重复推送同名标签；修复后递增版本号再发布。

### 给 Release 写说明（编写/更新发布日志）

推送标签触发 GitHub Actions 自动构建时，默认会使用“自动构建发布”作为占位说明。构建完成后，必须给 Release 补充正式的用户可见说明（Release Notes）：

1. **说明编写要求**：
   - 使用 Markdown 格式，以二级标题 `## <versionName>` 开头。
   - 分条目列出核心 Bug 修复、交互体验改进与配置兼容说明。
   - 重点说明触发场景、问题现象以及修复后的行为。

2. **更新 Release 说明方式**：
   - **方式一：通过 GitHub CLI 更新（推荐）**
     可以准备一个 Markdown 文件或者直接通过命令行更新：
     ```powershell
     # 从文件读取并更新 Release 说明
     gh release edit v1.5.4 --notes-file release-notes.md
     
     # 或直接传文本（支持换行）
     gh release edit v1.5.4 --notes "## 1.5.4`n`n- **模型快捷选择即点即存**：点击芯片立即写入 DataStore。`n- **退出设置自动保存**：拦截返回键自动持久化。"
     ```
   - **方式二：通过 GitHub 网页端更新**
     在仓库 Release 列表（`https://github.com/GodBook/ai-chat-android/releases`）中找到对应版本，点击右上角的编辑图标（Edit release），在 Description 文本框中粘贴更新说明并点击“Update release”。
   - **方式三：手动触发 Actions 时传入说明**
     在 GitHub Actions 界面选择 `发布 Android 更新`，点击 `Run workflow`，在 `release_notes` 输入框中填入完整的发布说明，工作流将直接使用该说明创建 Release 并写入 `latest.json`。

3. **核对更新结果**：
   ```powershell
   gh release view v1.5.4
   ```

## 九、在线更新链路

默认清单地址为：

```text
https://github.com/GodBook/ai-chat-android/releases/latest/download/latest.json
```

应用先下载并解析清单，再检查版本号是否高于当前版本、下载地址是否为 HTTPS、包名和签名是否匹配，最后校验 SHA-256。安装动作由 Android 系统 Package Installer 完成，应用不会静默安装。

更新 APK 必须使用相同包名、相同签名证书且版本号递增。卸载应用或清除数据会删除 Room、图片、DataStore 和 Keystore 内容，无法依靠当前备份规则恢复。

## 十、常见问题

### 请求提示接口地址无效

确认 Base URL 是完整 HTTPS 地址，例如 `https://api.deepseek.com/v1`。客户端会自动补 `/chat/completions`，不要重复配置错误路径。

### 默认模型过期后无法回复

默认模型 `deepseek-v4.1-flash-expires-on-0910` 是定时模型，到期后会返回 404 或带模型名的 400。“模型失效自动回退”开着时客户端会自动用 `deepseek-v4-flash` 重发一次；如果两次都失败，会展示第一个错误。只有服务端错误信息同时提到模型和“不存在/过期”一类字样（或直接 404）才会触发回退，普通请求错误不会换模型；不想要任何自动切换可以关掉该开关。

### 图片发送失败

确认设置中已开启“支持图片”，图片仍存在于应用私有目录，并且模型服务支持视觉消息格式。大图片应先压缩，避免请求体过大。

### 停止后无法继续

停止只会把助手消息标为 `INTERRUPTED` 并保留已生成文本。应使用该消息下方的“重试”；重试会按对应用户消息重新生成，不会自动接续 token 流。

### 后台音量键无响应

确认后台截图开关、悬浮窗权限、无障碍服务和通知权限，并确认设置里选择的快捷键与实际的按法一致（选了“音量上 + 下键”时，只按音量下键不会截图）。Android 10 还要重新授予屏幕捕获权限；部分厂商需要关闭电池优化。

### 更新检查失败

检查清单地址和下载地址是否 HTTPS，Release 是否同时包含 APK 与 `latest.json`，并确认清单中的版本号、包名、签名和 SHA-256 没有错配。

## 十一、后续开发约定

- 新增用户可见行为时同步更新 `README.md` 和本交接文档。
- 中文文件名可沿用现有项目风格，但类名、资源键和公共 API 保持清晰稳定。
- 不在业务代码中引入全局单例；依赖统一从 `AppContainer` 注入。
- Room、更新校验和网络安全相关改动必须配测试。
- 所有长耗时 IO 放在 `Dispatchers.IO`，Compose 层只负责状态展示和事件转发。
- 不要提交 `app/build`、签名文件、API Key、真实用户图片或临时更新 APK。

## 十二、交接检查清单

- [ ] 已能在本地使用 JDK 17 和 Android SDK 36 构建 Debug APK。
- [ ] 已运行单元测试和 Lint，并理解失败日志位置。
- [ ] 已确认 `com.example.aichat`、签名证书和版本递增规则。
- [ ] 已获得 GitHub 仓库和 Actions 管理权限。
- [ ] 已确认四个签名 Secret 仍然有效，但没有把 Secret 写入文档或代码。
- [ ] 已在测试设备验证普通文本、图片、停止、重试、导出和清空。
- [ ] 已在至少一台 Android 10 或以上设备验证后台截屏权限链路。
- [ ] 已确认 Release 包含 `ai-botoy-版本号.apk` 和 `latest.json`。
- [ ] 已确认应用内“检查更新”能发现最新版本。

本文件不保存任何生产凭据。凭据、签名文件和仓库权限应通过团队密码管理器和 GitHub 权限系统单独交接。
