# AI BOTOY 1.6.4 更新规划与设计文档

- **目标版本**：`1.6.4`
- **目标构建编号**：`versionCode 26`
- **核心主题**：**数据安全、无损跨机迁移、精美对话长图与系统级维护**
- **适用平台**：Android 10 至 Android 16（API 29 - 36）

---

## 一、版本规划背景与设计动机

在前序版本中，AI BOTOY 已经完成了基础体验与视觉框架的全面升级：
- `1.6.0` 引入了深度思考可视化（DeepSeek Reasoner / R1）、消息操作栏与快捷模型切换；
- `1.6.1` 完善了会话批量管理（多选、批量删除与安全资源级联清理）；
- `1.6.2` 重构了设置界面分组卡片体系，并让当前版本徽标一目了然；
- `1.6.3` 支持了短按快速新建/长按命名、自定义主题色系与会话分组折叠。

然而，在日常实际使用和用户换机场景中，存在以下核心痛点：
1. **换机与数据丢失痛点**：由于项目安全规范关闭了 Android 系统的云端整机备份与设备迁移（防止明文 API Key 泄漏），用户在更换手机、刷机或误卸载重装后，无法将珍贵的历史对话和截图记录转移到新设备。
2. **内容分享单一**：当前对话仅支持“系统分享发送纯文本”，缺乏美观的排版与视觉结构；在分享至社交媒体（微信、即刻、朋友圈、论坛等）时，无法生成类似大厂应用的高清长图卡片。
3. **模型配置排障成本高**：用户在设置中自定义 Base URL 或 API Key 时，若填错地址或密钥失效，往往只有在聊天窗口实际发送消息失败后才能发现；缺乏前置的“一键连通性测试”能力。
4. **存储资源累积**：长期进行截屏问答和图片对话会产生本地私有图片占用，需要向用户提供直观的空间统计与无损清理能力。

因此，**1.6.4 版本**被定位为 **“数据流转与分享赋能”** 的关键里程碑版本。

---

## 二、1.6.4 核心功能全景

```
AI BOTOY 1.6.4
├── 1. 数据安全与跨机迁移 (Backup & Restore)
│   ├── 全量备份导出（.zip 包含完整 JSON 数据 + 聊天原图文件夹）
│   └── 导入恢复（追加合并模式，自动生成新 UUID，防冲突重命名图片，Room 事务落盘）
│
├── 2. 精美长图与多样化分享 (Export & Share)
│   ├── 高清对话长图生成 (PNG)（精致 M3 卡片排版、时间戳、AI 徽章、可选思考链）
│   ├── 标准 Markdown (.md) 文件导出（支持 GFM 语法、代码块与可折叠思考块）
│   └── 传统纯文本分享保留 (TXT)
│
├── 3. 模型接口连通性测试 (Connection Probe)
│   ├── 设置页「测试连接」交互按钮（带旋转进度指示）
│   ├── 轻量探测请求与真实 RTT 耗时测算（毫秒级延迟）
│   └── 针对性错误诊断（区分 401 密钥失效、404 模型错误、网络超时等）
│
└── 4. 设置页「数据与存储」新卡片 (Data & Storage)
    ├── 本地存储占用统计（会话数、消息条数、图片缓存体积）
    ├── 备份与恢复操作主入口
    └── 孤立无引用废弃图片一键清理
```

---

## 三、各功能模块详细设计与技术实现

### 1. 全量数据备份与跨机恢复（Backup & Restore）

#### 1.1 备份包结构设计
备份文件采用通用的 `.zip` 容器格式，命名规则为 `ai_botoy_backup_YYYYMMDD_HHmmss.zip`：
```text
ai_botoy_backup_20260910_183000.zip
├── manifest.json                  // 备份清单与元数据
├── data.json                      // 结构化会话与消息完整数据
└── images/                        // 包含的所有本地聊天图片文件
    ├── chat_img_001.jpg
    └── chat_img_002.png
```

- **`manifest.json` 规范**：
  ```json
  {
    "appVersion": "1.6.4",
    "versionCode": 26,
    "schemaVersion": 5,
    "exportedAt": 1757497200000,
    "conversationCount": 8,
    "messageCount": 126,
    "imageCount": 15
  }
  ```
- **`data.json` 规范**：
  包含所有 `ChatConversation`（含 `groupName` 等分组信息）以及其关联的 `ChatMessage`（保留 `thinkingContent`、`thinkingDurationMs`、`status`、`imagePaths` 相对映射等）。
  > **安全红线**：备份数据中绝不包含 API Key！新设备需在设置中单独填入 API Key。

#### 1.2 导入合并策略（追加合并模式）
为防止覆盖或损坏用户在新设备上已经产生的新会话，导入统一采用 **追加合并模式（Append & Merge）**：
1. **解压校验**：将用户通过 Android 系统 SAF 文件选择器（`ActivityResultContracts.OpenDocument`）选择的 Zip 文件解压到临时缓存目录，验证 `manifest.json` 与 `data.json` 格式完整性。
2. **UUID 冲突隔离**：
   - 遍历导入会话，若该 `conversationId` 在本机已存在，生成新的 UUID，并建立 `oldId -> newId` 映射关系；
   - 遍历消息，更新所属的 `conversationId`，消息自身分配全新 UUID。
3. **图片物理迁移与防冲突重命名**：
   - 将 `images/` 中的图片拷贝至本机沙盒 `context.filesDir/chat-images`；
   - 若目标文件名已存在，重命名为 `imported_<timestamp>_<filename>`，并同步更新对应消息的 `imagePaths`。
4. **Room 数据库原子写入**：
   - 在 `DefaultChatRepository` 中通过 `chatDatabase.withTransaction { ... }` 一次性完成所有会话与消息的插入。
5. **资源清理与 UI 刷新**：
   - 彻底删除临时解压目录；
   - 通知 `MainViewModel` 重新加载当前会话列表，弹出 Toast 提示：“成功恢复 X 个会话与 Y 条消息”。

---

### 2. 高清对话长图生成与多样化导出（Export & Share）

#### 2.1 导出入口重构
在 `聊天界面.kt` 顶栏右上角的更多菜单（`MoreVert`）中，原有的“导出聊天”升级为多格式导出子项：
- **生成对话长图**（生成 PNG 并预览分享）
- **导出为 Markdown (.md)**
- **导出为纯文本 (.txt)**

#### 2.2 高清对话长图渲染架构（Canvas 离屏绘制）
无需引入庞大的第三方截屏库，利用 Android 原生 `android.graphics.Canvas` / `Picture` 或 Compose 离屏测量绘制：
1. **视觉规范设计**：
   - **宽度基准**：固定宽度为 `1080px`（高清晰度适配所有主流设备与社交平台分享）；
   - **头部 Header**：
     - 应用品牌 Logo + “AI BOTOY” 标题
     - 当前会话名称、生成模型标签（如 `DeepSeek Reasoner`）与导出时间
     - 精致的 Material 3 分割线与装饰性弧度
   - **消息气泡流**：
     - 用户消息：右上对齐，沉浸主色调背景，圆角弧度 `20dp`；
     - AI 助手消息：左侧对齐，柔和卡片背景；
     - 思考过程卡片（Thinking）：若包含思考链，渲染为浅色微光卡片，带“深度思考耗时 Xs”标签；用户可在导出前选择是否包含思考过程；
     - Markdown 渲染：段落、标题、有序/无序列表、引用框及代码块（深色代码背景底框）；
     - 本地图片消息：保真等比缩放并圆角裁切居中展示；
   - **底部 Footer**：
     - “— 对话由 AI BOTOY 生成 —” 精美水印字样与简约品牌标语。
2. **分享与存储**：
   - 生成位图保存至应用的私有外部缓存目录 `context.cacheDir/share_images/chat_share_<time>.png`；
   - 通过 `FileProvider` 获取安全 `content://` Uri，启动 `Intent.ACTION_SEND`（MIME 类型 `image/png`），唤起系统标准分享面板（支持一键保存到系统相册、分享到微信/QQ/钉钉等）。

#### 2.3 标准 Markdown 导出 (.md)
将会话按照通用 Markdown 语法编排：
```markdown
# 对话标题：React 性能优化实战

> 导出时间：2026-09-10 18:30  
> 所用模型：deepseek-reasoner

---

### 👤 用户
请总结一下 React 常见的性能优化手段有哪些？

### 🤖 AI (DeepSeek Reasoner)
<details>
<summary>🧠 深度思考过程（耗时 12 秒）</summary>

用户询问的是 React 性能优化方案，我需要从组件渲染控制、状态下沉、计算缓存、代码拆分等多个维度进行系统性归纳...
</details>

在 React 应用中，核心的性能优化策略主要包含以下几大维度：

1. **避免不必要的重新渲染**
   - 使用 `React.memo` 避免父组件更新引发的子组件级联渲染；
   - 合理使用 `useMemo` 与 `useCallback` 缓存计算开销大的衍生状态和函数句柄。
...
```
导出的 `.md` 文件同样通过系统分享面板发送，用户可无缝转存至 Obsidian、Notion 或本地文件管理器。

---

### 3. 模型接口连通性实时测试（Connection Probe）

#### 3.1 业务流程
在 `设置界面.kt` 的「模型与接口」卡片中，在“模型名称”与“快捷选择”下方新增 **「测试连接」** 交互按钮：
1. **防抖与前置校验**：点击前先校验 Base URL 格式（必须 HTTPS）以及 API Key（不能为空）。
2. **轻量探测请求（Probe Request）**：
   - 构造单轮极小 payload 发送至 `${baseUrl}/chat/completions`：
     ```json
     {
       "model": "<用户当前填写的模型名称>",
       "messages": [{"role": "user", "content": "ping"}],
       "max_tokens": 1,
       "stream": false
     }
     ```
   - 记录请求发起时间与接收响应时间差，计算 RTT 往返延迟。
3. **状态与结果展示**：
   - **测试中**：按钮变为加载状态，显示小尺寸 `CircularProgressIndicator` 与“正在测试连通性...”。
   - **测试成功**：在按钮下方显示绿色提示条，显示 `连接成功 · 延迟 168ms · HTTP 200`。
   - **测试失败**：以红色警示框清晰解读服务端返回的错误原因：
     - `401 Unauthorized`：提示“API Key 无效或未授权，请检查密钥是否正确”；
     - `404 Not Found`：提示“模型名称不存在或 Base URL 路径错误”；
     - `429 Too Many Requests`：提示“账户额度不足或达到并发限制”；
     - `ConnectException / Timeout`：提示“网络连接超时，请检查接口地址与网络连通性”。

---

### 4. 设置页「数据与存储」新卡片（Data & Storage Management）

在 `设置界面.kt` 中，原有 4 个卡片扩展为 5 个独立 Material 3 卡片：
1. 模型与接口（含新「测试连接」）
2. 对话偏好（主题色、思考链折叠）
3. 后台截图助手
4. **数据与存储（1.6.4 新增）**
5. 关于与更新

#### 4.1 卡片内容规划
- **存储占用概览面板**：
  - 会话总数与消息总条数（如 `15 个会话 · 328 条消息`）；
  - 聊天图片缓存占用空间（计算 `context.filesDir/chat-images` 下文件总大小，以 MB 格式化展示）。
- **全量备份与迁移操作行**：
  - **导出全量备份**：点击唤起 SAF 保存对话框，将所有会话、消息及图片打包为 `.zip` 保存到本地或云盘；
  - **恢复备份数据**：点击唤起 SAF 选择器，选择 `.zip` 文件并执行安全追加合并。
- **存储空间清理**：
  - **清理无效图片缓存**：后台协程比对数据库中所有消息字段的 `imagePaths`，查找沙盒目录中已不再被任何会话引用的孤立废弃图片，安全批量删除并释放手机空间，提示“已清理 X 个孤立文件，释放 Y MB 存储”。

---

## 四、数据架构与兼容性保障

### 1. Room 数据库与模型兼容
- 1.6.4 的功能依托现有的 `ChatConversation` 与 `ChatMessage` 结构（包含 1.6.0 引入的思考链字段与 1.6.3 引入的 `groupName` 字段）。
- **无需升级 Room 数据库版本**（保持 Database Version 5），降低迁移风险，100% 保证平滑兼容。

### 2. 权限与系统兼容性（Android 10 - 16）
- **文件导出/导入**：全面采用 Android 系统推荐的 Storage Access Framework（SAF）与 `ActivityResultContracts.CreateDocument` / `OpenDocument`，无需申请危险的外部存储读写权限（`WRITE_EXTERNAL_STORAGE`），完美适配 Android 10 到 Android 16 的 Scoped Storage 分区存储规范。
- **长图分享**：通过标准的 Android `FileProvider` 授予只读临时 URI，无权限泄露隐患。

---

## 五、发布计划与版本发布日志（Release Notes）

### 1. 版本号递增
- `versionCode`: `25` -> `26`
- `versionName`: `"1.6.3"` -> `"1.6.4"`

### 2. 发布说明（Release Notes 文案）

#### 中文版（供 GitHub Releases & 客户端更新清单 `latest.json` 使用）
```markdown
## 1.6.4

### 亮点功能
- **全量数据备份与跨机恢复**：
  - 支持将全部聊天会话、消息记录与本地原图一键打包导出为 Zip 备份文件；
  - 提供智能追加合并导入能力，换机或重装后可无损恢复全部历史数据，防止重复覆盖。
- **精美对话长图与多样化导出**：
  - 对话界面支持一键生成高清对话长图（PNG），包含优雅的 M3 卡片排版、时间戳、AI 徽标与思考链展示；
  - 新增标准 Markdown (.md) 格式文件导出，保留完整代码块与可折叠思考块；
  - 保留原有纯文本系统分享。
- **设置页模型连通性实时测试**：
  - 在「模型与接口」中新增「测试连接」功能，秒级探测网络握手延迟并验证 API Key 与模型可用性；
  - 提供针对 401、404、429 与连接超时的人性化诊断提示。
- **设置页新增「数据与存储」卡片**：
  - 实时统计应用会话数量与图片存储占用空间；
  - 提供“清理无效图片缓存”功能，一键扫描并清理已被删除会话残留的历史孤立图片，安全释放手机存储。
```

#### English Version (For International Users)
```markdown
## 1.6.4

### Highlights
- **Full Data Backup & Restore**:
  - Export all conversations, message history, and local chat images into a single `.zip` backup package.
  - Safe append & merge import strategy to restore all history seamlessly across devices without data loss.
- **Long Image Card & Markdown Export**:
  - Generate crisp, beautifully styled PNG long screenshot cards of any conversation for easy sharing.
  - Export chat records as standard Markdown (`.md`) files with syntax highlighting and collapsible reasoning blocks.
- **Model Connection Probe**:
  - Test model connectivity, API key validity, and network latency directly from Settings.
  - Clear error diagnosis for 401 Unauthorized, 404 Not Found, and rate-limiting issues.
- **New Data & Storage Management Card**:
  - View real-time storage statistics of chat messages and cached images.
  - One-tap orphan image cache cleanup to safely reclaim device storage.
```

---

## 六、开发与验证执行清单（Checklist）

- [ ] **数据层**：
  - [ ] 编写 `BackupRestoreManager`：实现基于 Zip 的打包（JSON + 本地图片）与追加合并恢复逻辑；
  - [ ] 编写单元测试验证各种边界场景（UUID 冲突、图片重命名、空会话包、损坏的 Zip 格式校验）；
  - [ ] 实现 `cleanupOrphanImages()` 方法，过滤扫描并清理无消息引用的废弃本地图片。
- [ ] **网络层**：
  - [ ] 在 `OpenAiCompatibleClient` 中实现 `probeConnection(baseUrl, model, apiKey)` 轻量测试方法，捕获并格式化 RTT 延迟及具体 HTTP 错误。
- [ ] **UI 渲染与交互**：
  - [ ] 在 `设置界面.kt` 中添加「测试连接」按钮与状态反馈组件；
  - [ ] 在 `设置界面.kt` 中增加「数据与存储」卡片（包含空间统计、备份/恢复、清理缓存）；
  - [ ] 在 `聊天界面.kt` 顶栏菜单中扩展导出面板；
  - [ ] 实现长图渲染生成器（`ChatImageExporter`），生成位图并通过 `FileProvider` 唤起分享；
  - [ ] 实现 Markdown 文本生成器（`ChatMarkdownExporter`）。
- [ ] **测试验证**：
  - [ ] 运行 `./gradlew.bat :app:testDebugUnitTest --no-daemon` 确保全量单测通过；
  - [ ] 在 Android 10（API 29）与 Android 15/16 设备上验证 SAF 文件读写与长图分享。
- [ ] **发布上线**：
  - [ ] 递增 `app/build.gradle.kts` 版本至 `versionCode 26` / `versionName "1.6.4"`；
  - [ ] 执行 `.\publish.ps1 -VersionName "1.6.4" -NotesFile "..."` 完成正式发布与 GitHub 同步。
