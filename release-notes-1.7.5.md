## 1.7.5

### 截图助手
- 新增后台“截图助手”功能（可在设置中心自由选择开启/关闭）。
- 开启后，后台截屏识图问答在 AI 输出答案后，自动在后台调用语音播放朗读 AI 的回答内容。
- 朗读过程中，用户按下音量上键可立刻结束语音播放，并拦截按键避免系统音量跳增。
- 朗读过程中若再次进行后台截图，也会自动中断上一轮语音，无缝开启新一轮识别。

### 上下文透视
- 输入栏新增上下文入口，查看本次实际携带的完整历史问答、角色设定、结论卡、文字量与图片数；可为下一次发送排除指定轮次。
- 显示超出历史轮数、字符预算、未完成回答及未选中版本等原因。轮数只统计历史问答，本次问题独立追加。
- 回答版本选择持久保存，后续提问使用选定版本；所选版本不可用时提示回退到完成版本。搜索定位旧回答也同步选择。
- 消息“更多”可查看当次请求清单，保存模型、角色快照、所选历史和卡片修订；联网完成后记录搜索查询与最终资料。源消息删除后正文不再显示。旧回答显示未记录清单。

### 可追溯结论卡
- 从 AI 回答的“更多 → 保存结论卡”摘取资料，或手动新建结论、约束、待办；支持编辑、筛选、查找与返回保存时的回答版本。
- 主动选择卡片用于下次提问，不自动发送、不自动调用模型。标题最多 40 字符，正文最多 2,000 字符；每次最多 8 张，正文合计最多 8,000 字符。
- 支持通过系统保存器导出 Markdown，卡片纳入 ZIP 备份。删除会话时可选择同时删除卡片或保留到全部结论卡。
- 卡片出处不代表事实已核验；编辑后的内容与原始摘录分别保留。临时对话不读写结论卡或持久化请求清单。

### 续聊包
- 在结论卡中选择成果，填写下一步目标并预览服务商、模型与角色，创建新聊天并填入草稿，编辑后手动发送。
- 沿用来源会话配置，配置失效时提示；草稿保存到本机，重启后可恢复。支持单独导出续聊包 Markdown。

### 数据与版本
- 本地版本 1.7.5（versionCode 39），Room 10 升级至 11，保留原聊天；ZIP 备份格式升级至 9，支持读取旧备份并重映射新资料关联。
- 文字量是客户端字符统计，不等于模型精确 Token。联网可能增加资料；本次必带资料超过客户端预算时提示缩短，不静默丢弃。
- 已发出的聊天正文、外部导出和旧备份不会因删除卡片而追溯删除。应用临时对话继续沿用原有内存语义。

本说明对应本地待发布构建；远程 Release 和在线更新清单需正式发布后另行核对。
