# 1.7.7（versionCode 41）

- 简版回答方块改为更浅的灰色（#D2D2D2），从 32×19 dp 缩为 20×10 dp。保持原来的分区、中心位置和约 1 秒显示时长。
- 设置 → 截图助手新增“纯语音播报”，默认关闭。开启后自动朗读回答，不创建回答悬浮窗、简版方块，不显示播放器、回答通知或错误 Toast。错误改为语音提示；语音引擎不可用或媒体静音时无法发声，可回设置查看错误并测试语音。
- 纯语音优先于简版模式，无需开启悬浮窗权限。按音量上键停止，下一次截图也会停止上一段语音；截图及会话仍保留在应用历史中。
- 新增可选“Root 截图”，默认关闭。通过用户授权的 su 执行固定 screencap 命令，图像通过管道读取，不写入公共相册。首次测试需在 Root 管理器授权；超时或失败直接提示，不自动切换到无障碍截图或屏幕录制。
- Root 模式仍需无障碍服务接收快捷键，不会隐藏应用安装信息、Root 授权或无障碍服务，也不保证截图行为无法被第三方软件检测。未修改其他软件、系统检测回调或安全窗口限制。
- Android 10 的普通截图路径仍保留系统要求的屏幕捕获授权和前台服务通知；纯语音只关闭应用的回答显示，不能消除系统提示。

## 安装后验证

1. 开启简版模式，确认 A–D 和判断题方块位置不变，尺寸更小、颜色更浅。
2. 在设置中测试语音并调好媒体音量，再开启“纯语音播报”；即使同时开启简版模式，也只播报声音。
3. 关闭悬浮窗权限，确认纯语音仍能通过快捷键工作；用音量上键停止播放。
4. 如需 Root 通道，开启“Root 截图”并完成首次授权。授权拒绝或超时时应收到错误，不应改用其他通道。
5. 等待 AI 回答期间切换纯语音开关，确认结果按最新设置输出；重新进入设置确认两个开关保留。

Root 兼容性、实际语音播放及第三方检测情况需在用户手机上验证。
