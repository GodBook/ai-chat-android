# AI BOTOY 本地发布签名配置文件模板 (方案 A)
#
# 使用方法：
# 1. 将本文件复制一份并重命名为 local.signing.properties
# 2. 将生产签名证书 (如 signing.keystore) 放在项目根目录 (或指定绝对路径)
# 3. 填入你的证书别名和密码
# 
# 注意：local.signing.properties 与 *.keystore 已在 .gitignore 中全面忽略，绝对不会提交到 Git。

KEYSTORE_PATH=signing.keystore
KEY_ALIAS=
KEYSTORE_PASSWORD=
KEY_PASSWORD=
