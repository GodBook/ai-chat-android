# AI BOTOY 1.7.5 更新规划与设计文档

> 版本主题：让每次提问有据可查，让重要结论可以继续使用。
>
> 文档日期：2026-09-12。状态：研究与待开发方案，本文不代表功能已经实现或发布。
> 基线：本地源码 1.7.4 / versionCode 38 / Room 10。
> 目标：1.7.5，建议 versionCode 39；实际发布前应确认编号没有被其他构建占用。
> 范围：文本、图片、文件资料及普通聊天工作流；不规划语音输入、朗读或语音通话，也不要求移除已有语音功能。

## 1. 结论与版本定位

建议 1.7.5 主打两个核心功能：**上下文透视**与**可追溯结论卡**。完成后，再加入基于结论卡的**续聊包**。

用户能在发送前看清 AI 将收到哪些历史，主动排除无关轮次；能把长回答中的结论、约束和待办保存下来，回到原文核对，再明确选择是否用于后续提问。

创新集中在三件事：**可见、可控、可复用**。这是一套基于现有聊天能力形成的工作流，不依赖另建模型后端、向量数据库或后台自动代理。这里的“创新”指对本产品的体验改进，不宣称行业首创。

建议发布范围：

| 优先级 | 功能 | 用户价值 | 发布约束 |
| --- | --- | --- | --- |
| P0 | 上下文透视与回答版本一致性 | 知道实际发送什么，减少带错历史的情况 | 必须完成 |
| P0 | 可追溯结论卡 | 保存有用结论，带着出处再次使用 | 必须完成 |
| P1 | 续聊包 | 将选定成果带入新聊天，继续推进任务 | 核心能力通过验收后加入；否则顺延 |
| P2 | 双模型对照、自动摘要、文件证据定位 | 扩展探索与研究能力 | 不纳入本次发布承诺 |

## 2. 本地研究依据

### 2.1 已有能力与真正的增量

| 现状 | 本地依据 | 对 1.7.5 的意义 |
| --- | --- | --- |
| 1.7.3 已支持文件提取、预览、追问和系统分享 | `release-notes-1.7.3.md`、`DocumentAttachment.kt`、`DocumentImporter.kt` | 不重复把“上传文件”当新功能 |
| 1.7.4 已支持分页搜索、筛选、分支定位及代码／CSV 导出 | `release-notes-1.7.4.md` | 新版本应解决找到信息之后怎样继续使用 |
| 历史请求按消息数窗口和字符预算裁剪 | `DefaultChatRepository.kt` | 用户设定的轮数不等于全部历史必然进入请求，需要解释实际入选结果 |
| 界面选中的回答版本由 ViewModel 管理；仓库组装历史时未传入该选择 | `MainViewModel.kt`、`MessageMappers.kt`、`DefaultChatRepository.kt` | 需要统一“正在阅读”和“用于后续提问”的版本 |
| 消息实体已有 requestId、来源结果、usage 等信息 | `ChatMessageEntity.kt` | 可以关联具体回答并复用已有用量信息 |
| 当前持久化模型没有结论卡及请求上下文快照 | `ChatDatabase.kt`、消息和会话 Entity | 新增能力需要迁移、备份恢复和删除语义 |

观察到的请求裁剪逻辑使用 48,000 字符预算，图片按每张 2,000 的固定成本参与计算，并保留最近一条消息。这些是当前客户端的启发式规则，不是模型真实 Token 上限，也不保证请求一定不会超出服务商限制。界面不应把它包装成“精确剩余 Token”。

另一个需要区分的概念：1.7.3 把提取文字随消息保存，原始文件不留存。因此本次能追溯到的是聊天中的实际摘录，不能承诺重新打开任意原始 PDF 页或读取当时没有提取的全文。

### 2.2 研究范围与证据边界

本方案以本地发布记录、实体结构和请求组装代码为依据。尝试检索外部产品资料时未获得可用页面内容，因此没有纳入无法核实的竞品功能、市场排名或“最新行业趋势”。本文件中的优先级、交互规则、容量与工期均为设计建议，尚未通过用户研究、开发或设备实测。

旧的 `1.7.0更新规划与设计文档.md` 仅作为项目规划风格参考。历史规划中的功能描述不直接视为已经实现，当前行为以源码和版本说明交叉核对。

## 3. 核心功能一：上下文透视

### 3.1 使用场景

用户围绕一份方案聊了很多轮，期间讨论过两个方向，最后希望继续方向 A。现在他可以阅读旧版本，但不容易判断新提问实际携带了哪个回答、多少历史，以及早期文件资料是否还在窗口内。

1.7.5 在输入栏上方增加轻量入口：

```text
上下文 · 6 轮历史 · 2 张结论卡     查看
[继续完善这个方案……                     ]
```

数字必须来自实际请求计划。文件摘录仍按所属消息计入历史，不重复累计为独立附件；本次新附加的文件和图片单独列示。

点击“查看”打开底部面板，大段内容再进入全屏详情：

```text
本次将发送
角色设定        产品顾问
历史对话        6 轮                         展开
结论卡          2 张                         管理
本次问题        继续完善这个方案……
本次附件        需求说明.md                   预览

未携带          3 轮超出窗口、1 轮手动排除
文字量          约 18,400 字符；图片 1 张
联网搜索        开启，搜索资料将在检索完成后加入

                                      完成
```

### 3.2 交互与行为规则

1. **预览与发送共用同一份请求计划。** 禁止 UI 自己估算一套列表、仓库发送另一套列表。编辑问题、切换模型／角色／分支或附件后，重新计算预览。
2. **排除以历史问答轮次为单位。** 不允许单独保留一条失去对应提问的助手消息。首版只影响下一次发送，发送成功后清除；校验失败保留，切换会话不串入其他聊天。
3. **历史不因排除而删除。** 被排除的轮次继续可以搜索和导出。当前问题不可排除；角色设定通过原有面具入口修改。
4. **明确展示排除原因。** 区分超出轮数、超出字符预算、手动排除、未完成回答与非选中版本。
5. **回答版本统一。** 每个 requestId 使用明确的 assistantMessageId 记录选择。重启后恢复选择；搜索定位旧分支也同步选择，并提示“后续提问将使用此版本”。已发出的历史请求保持原样。
6. **无有效版本时回退可解释。** 选中版本已删除或未完成时，优先使用该轮最后一个完成版本并提示；没有完成版本则不携带该轮助手内容。完整轮次的裁剪、失败轮次的处理应由统一规则负责，不能因保留半轮而意外改变语义。
7. **窗口按完整历史轮次计算。** 当前问题独立追加，“最近 8 轮”表示最多 8 个有效历史问答对，再加本次问题。新发、重试和重新生成统一定义，避免各入口算出来不同。
8. **联网分阶段说明。** 发送前只能展示搜索开关及待补充状态；检索完成后记录最终加入的来源。明确区分发送给搜索服务的查询／摘录与发送给聊天服务的最终上下文。
9. **不显示伪精确价格。** 展示实际字符量、图片数和已返回的 Token usage；缺失 usage 时显示“服务未返回”。首版不预测费用，也不宣称节省固定比例。

### 3.3 已发送请求的可追溯性

助手消息“更多”中增加“查看本次上下文”。记录本次实际使用的消息 ID、分支 ID、结论卡修订版本、模型名、角色设定快照、手动排除项及最终搜索摘录。排除密钥、Authorization 和自定义认证头。

普通聊天保留本地请求清单；可变的角色设定和结论卡保留当次文本快照。历史消息通过 ID 引用：如果用户之后删除原消息，清单显示“来源已删除”，不以隐藏副本保存已删除正文，也不再承诺完整重放。清单不存在的旧回答显示“此回答未记录上下文”。

重试与重新生成基于目标问题之前的历史，不能混入后续消息。默认复用当次仍可解析的选择及卡片快照，联网仍按现有规则刷新；若资料被删除或配置变化，显示差异并让用户查看当前请求计划，不能声称完全复现旧请求。

### 3.4 验收标准

- 用模拟服务捕获请求，逐项比对预览和实际消息顺序、角色、内容及所选分支。
- 先生成 A、再生成 B，切回 A 后追问；真实请求包含 A，不包含 B。应用重启后结果一致。
- 排除一轮后不发送该轮；历史列表、搜索和导出仍保留该轮。
- 超出轮数／字符预算时给出实际原因，不出现只有助手回复的孤立历史。
- 切换会话、旋转屏幕、取消发送和配置校验失败均不串上下文；生成中配置变化仅影响下一次请求。
- 联网前后展示状态准确，最终记录与发送给聊天服务的检索资料一致。

## 4. 核心功能二：可追溯结论卡

### 4.1 使用场景与创新点

一段回答里可能只有一句需求约束需要长期保留。例如“离线时允许编辑，恢复网络后再同步”。保存整段对话太重，反复复制又容易丢失来历。

结论卡保存用户明确挑选的内容，附带原始消息定位，并能按需加入下一次请求。它不是自动记住一切的全局记忆，也不是经过模型证明的事实。

三种类型只用于组织与阅读，不影响事实可信度：**结论、约束、待办**。

```text
约束 · 已编辑
离线时允许编辑，恢复网络后再同步。

来自：同步方案 · 回答版本 1 · 09-12
查看原文          编辑          用于提问
```

### 4.2 最小交互闭环

1. 助手消息菜单增加“保存结论卡”，进入编辑页，预填当前回答文字，用户选择保留范围、标题与类型后保存。暂不依赖 Android 选文菜单的定制集成。
2. 支持手动新建卡片，标为“用户记录”，没有伪造来源。
3. 聊天“更多 → 结论卡”查看当前会话的卡片；列表支持标题／正文关键词过滤和类型筛选。首页“更多 → 全部结论卡”支持跨会话查找，不新增常驻底部导航。
4. 点击“查看原文”复用消息定位能力，按固定消息 ID 打开保存时的回答版本。
5. 点击“用于提问”将卡片放入输入栏的待发送清单，允许移除和核对后再发送。发送后清空选择；失败保留草稿语义。
6. 首版不自动调用模型生成卡片。超过长度上限时让用户缩短，不静默截断，也不增加隐藏请求。

建议限制：标题最多 40 个字符，正文最多 2,000 个字符，单次最多携带 8 张且正文合计最多 8,000 个字符；都属于待实测的产品默认值。超过数量或长度时明确提示，不静默省略。

### 4.3 来源、编辑与删除

- 保存时固定具体 sourceMessageId、sourceRequestId 和来源会话；不能只存会变化的“第几个版本”。
- 卡片正文与最初摘录分开保存。编辑后标为“已编辑”，并可查看保存时摘录；手动改写的内容不能继续显示为逐字原文。
- 卡片的“来源”表示出处，不代表验证通过。来自模型的内容应标明“AI 回答摘录”；从回答复制来的链接不自动获得已核验标签。
- 删除原消息后保留用户主动保存的卡片，来源显示“原消息已删除”。删除提示明确说明独立保存的卡片仍存在。
- 删除整个会话时允许同时删除其卡片，默认选中；取消后将卡片保留到“全部结论卡”，解除失效会话关联，并保存原会话标题快照。
- 删除卡片时清理未发送草稿中的引用及历史请求清单里的卡片内容快照；已实际发出的用户消息正文仍属于聊天记录，提示用户可另外删除对应消息。已导出的文件和备份不会被追溯删除。
- 临时聊天不开放保存结论卡或读取全局卡片，避免跨越当前内存会话边界。

### 4.4 进入模型的方式

把选中卡片渲染为本次用户消息中的独立资料段，包含卡片类型、标题、正文、来源标签；不提升为 system 指令。用户在预览中能看到完整内容。

发送顺序建议为：角色设定 → 选中的有效历史 → 本次问题及结论卡／新附件 → 本次检索资料。卡片与新附件先占用预算，超预算时减少历史并解释；如果本次必带资料本身已超预算，要求缩短或移除后发送，禁止静默丢卡。字符预算只是客户端保护，服务端超限错误仍需正常展示。

资料段明确声明其内容是待参考材料，不能覆盖用户问题和应用系统约束。这只能降低指令混淆风险，不能承诺从根本上杜绝提示注入。

由于已发送正文包含当次卡片资料，现有聊天搜索与 Markdown 导出可以保留当次使用内容；请求清单再保存结构化关联。后续追问不另外自动附加同一组卡片，避免重复注入。

### 4.5 验收标准

- 从旧回答版本保存卡片，随后切换版本；卡片仍定位保存时的版本。
- 编辑卡片不修改原聊天，旧请求不因当前卡片修订而改变。
- 未点击发送前不产生模型请求；已选卡片只发送到当前明确配置的服务。
- 删除原消息、删除整个会话、删除卡片三种操作分别符合上述保留和清理规则。
- 普通聊天卡片可搜索、导出为 Markdown、纳入 ZIP 备份恢复；临时聊天不出现持久化记录。

## 5. 增强功能：续聊包（P1）

### 5.1 具体体验

在结论卡列表勾选若干卡片，点击“带到新聊天”。打开预览页，让用户填写下一步目标，展示即将携带的结论、约束和待办。

例如，旧聊天已经确定了功能范围、交付限制和三个未解决问题。新聊天无需复制几十轮历史，只带这些明确选择的内容就能继续工作。

“创建并填入草稿”只创建聊天和草稿，不自动发送。服务商与模型默认沿用来源会话的有效配置，并在创建前展示；配置不可用时先选择可用配置，不能悄悄改用其他服务商。角色沿用当前有效面具并允许修改。

### 5.2 实现边界

- 首版是本地确定性拼装，不做模型摘要，不生成用户没有选中的结论。
- 草稿保存可编辑的内容副本及来源标记，不依赖之后仍存在的原卡片。
- 沿用结论卡数量和总长度限制，不附带原文件、历史图片、全部聊天记录或任何密钥。
- 用户取消预览不创建空会话；重复点击不会创建多个会话；进程恢复后不会自动发送。
- 新聊天后续修改不回写来源会话；可将续聊包单独导出为 Markdown，使用系统保存器选择位置。

验收重点是“只携带所选内容、草稿可恢复、创建幂等、无自动请求”。此项可以独立顺延，不影响两个 P0 功能发布。

## 6. 为什么暂不选择其他方向

| 候选方向 | 本次判断 | 原因 |
| --- | --- | --- |
| 双模型同时回答／自动裁判 | 后续独立版本研究 | 涉及并发状态、双份消耗、失败隔离及可比性，容易放大范围 |
| 全自动长期记忆 | 暂缓 | 需要定义自动写入、冲突、遗忘与跨会话传递；先验证用户主动保存的价值 |
| 文件知识库／向量检索 | 暂缓 | 当前仅保存提取文字，尚缺结构化原文块、检索与出处校验基础 |
| 自动生成任务并后台执行 | 暂缓 | 需要新增调度、权限及外部操作边界，与当前小版本目标距离较大 |
| 再增加模板或主题 | 不作为主打 | 能改善局部体验，但不能解决上下文不透明和成果复用问题 |

## 7. 技术落点与数据兼容

以下是建议设计，不是现有类型声明。实现阶段应保持依赖由 AppContainer 注入，IO 工作离开主线程。

| 模块 | 建议改动 |
| --- | --- |
| `data/model` | 新增 `ContextPlan`、`ContextExclusion`、`KnowledgeCard`；定义完整轮次与稳定分支选择 |
| `data/repository/DefaultChatRepository.kt` | 抽出统一请求规划器，发送／重试／重新生成复用；固化发送时快照 |
| `data/model/MessageMappers.kt` | 保留分支解析职责，以稳定消息 ID 与请求计划对接 |
| `data/local` | 卡片表、分支选择表、请求清单表及 DAO；补充事务与迁移 |
| `ui/MainViewModel.kt` | 管理按会话隔离的临时排除项、卡片选择、预览状态和失效恢复 |
| `ui/聊天界面.kt`、`ui/消息渲染.kt` | 增加上下文入口、消息操作及详情面板，保持输入框可用宽度 |
| `data/local/BackupRestoreManager.kt` | 增加新实体的导出、恢复、ID 重映射与旧包默认值 |
| `ui/export` | 增加卡片和续聊包 Markdown 格式化，沿用系统保存器 |

建议新增数据：

- `knowledge_cards`：id、可空 conversationId、sourceConversationTitle、可空 sourceMessageId／requestId、sourceExcerpt、title、body、kind、revision、createdAt、updatedAt。
- `conversation_branch_selections`：conversationId、requestId、assistantMessageId，前两项组成唯一键；恢复时验证所属关系。
- `request_context_records`：assistantMessageId 唯一关联，记录有界、带格式版本的请求计划；包含模型／角色文本快照、消息引用、卡片修订快照、裁剪原因和最终检索信息。不得保存整份原始 HTTP 请求。

建议 Room 10 → 11，保留原消息与会话字段语义；迁移只增表和必要索引，禁止通过删除重建数据库处理升级。旧消息没有上下文清单属于正常状态。

备份格式版本与 Room 版本分别管理，不能假设编号相同。新版本读取旧包时，新表默认空；新包显式声明格式版本。恢复时统一映射会话、消息、requestId、卡片和上下文关联，避免追加导入后指向原库的同名 ID。超出支持范围的格式明确报错，不能静默丢弃新数据；不承诺旧 APK 可以恢复新格式备份。

请求计划与卡片必须限制正文大小，并验证 JSON 枚举、数量和引用归属。卡片正文可随聊天资料导出；认证信息和内部请求头一律不进入这些实体。

## 8. 交付阶段与验收门槛

### 8.1 建议阶段

| 阶段 | 交付物 | 退出条件 |
| --- | --- | --- |
| A：请求一致性 | 统一上下文规划、稳定分支选择、关键请求测试 | 预览计划与捕获请求一致 |
| B：上下文交互 | 预览、单次排除、发送后清单 | 旋转、取消、失败与重试状态正确 |
| C：成果保存 | 卡片创建、编辑、定位、用于提问 | 形成完整使用闭环 |
| D：数据兼容 | Room 迁移、备份恢复、删除及导出 | 旧数据保留，关联正确，无隐藏正文残留 |
| E：可选增强 | 续聊包 | P0 验收完成后决定是否纳入 |
| F：发布验证 | 设备回归、正式说明与包校验 | 发布检查通过 |

粗略工作量：两个 P0 功能及兼容回归约 10–15 个工程人日，P1 约 2–3 个工程人日。假设由熟悉项目的 Android 开发者推进，不涉及大范围界面重构；这是规划估算，不是交付日期或性能承诺。

### 8.2 必测清单

| 类别 | 关键场景 |
| --- | --- |
| 请求正确性 | 分支 A/B 选择、完整轮次、窗口边界、卡片优先级、字符超限、图片成本 |
| 入口一致性 | 新发、失败重试、重新生成均使用统一规则，不携带目标问题之后的消息 |
| 生命周期 | 旋转、进程重建、切换会话、后台返回、生成时改变选择 |
| 数据 | 10→11 与旧版本完整迁移链、旧 ZIP、新 ZIP、重复导入、ID 冲突、损坏引用 |
| 删除 | 删除消息／会话／卡片后来源状态、草稿清理、请求快照清理 |
| 临时聊天 | 无卡片读写、无持久请求清单、退出后不出现在搜索与备份 |
| 界面 | 320dp 宽屏、大字体、深色模式、长标题与长代码不挤压输入栏 |
| 回归 | 文件问答、联网来源、分支搜索定位、代码／CSV 导出、停止和模型回退 |

性能验收建议：准备 1,000 条消息和 200 张卡片的本地固定数据集，测量打开预览、筛选及滚动；在同一设备比较更新前后，并记录设备型号、系统、数据量与冷／热启动条件。具体延迟阈值在阶段 A 测得基线后确定，不先写“毫秒级”“零卡顿”等宣传承诺。

发布时执行项目既有 JVM 测试、仪器测试和 Lint，并增加实际请求比对、迁移及备份恢复测试。本轮只编写规划文档，没有执行构建或把上述测试标记为通过。

## 9. 发布文案草案

> 下述文字仅供实现并验收后用于 Release；未完成的条目必须删除。P1 未交付时不写入发布记录。

### AI BOTOY 1.7.5：看清上下文，留下有用结论

- **上下文透视**：发送前查看本次携带的历史、回答版本与结论卡，临时排除无关轮次，了解哪些内容因窗口限制未被发送。
- **回答版本一致性**：切换历史回答版本后，后续提问使用你明确选择的版本；已发送回答可查看当次记录的上下文清单。
- **可追溯结论卡**：把重要结论、约束和待办保存为卡片，随时编辑、查找、返回来源，并选择加入新的提问。
- **数据与导出**：结论卡支持 Markdown 导出与备份恢复，覆盖安装保留已有聊天记录。
- **可选：续聊包**：将选中的卡片整理到新聊天草稿，核对并发送后继续讨论。

## 10. 后续实现时需要同步的文件

功能完成后，再更新 `README.md`、`项目交接文档.md`、新增 `release-notes-1.7.5.md`、更新 `app/src/main/assets/release-history.json` 与 `app/build.gradle.kts`。正式发布前核对版本号、正式签名、APK、在线清单和 SHA-256。

本次交付仅新增本规划文档，保持应用当前版本与发布记录的事实一致。
